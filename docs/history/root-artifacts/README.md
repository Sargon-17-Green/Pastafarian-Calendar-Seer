# Historical root artifacts

These files are preserved as evidence of earlier overlays/deltas and checksum snapshots. They are not one coherent current release package.

Do not reinterpret embedded paths or checksums against the repository's current layout. Current release integrity is defined by the release manifest/assets, attestations, and `docs/SUPPLY_CHAIN.md`.
